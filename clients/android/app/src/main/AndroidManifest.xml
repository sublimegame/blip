<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Tell the system this app requires OpenGL ES 3.0, this should match compiled BGFX version -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true"/>

    <!-- Permissions for Mixpanel -->
    <!-- This permission is required to allow the application to send events and properties to Mixpanel. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- This permission is optional but recommended so we can be smart about when to send data. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- This permission is optional but recommended so events will contain information about bluetooth state -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <!-- for haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- for Post Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!--  Used for importing files  -->
    <queries>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
        </intent>
    </queries>

    <!-- Use the following theme if using AppCompatActivity:
         android:theme="@style/AppTheme.NoActionBar.Fullscreen" -->
    <application
        android:name=".GameApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeCubzh">
        <!-- About android:configChanges="orientation"
           We do not want this activity to be destroyed and re-created when screen rotates,
           since it is a container for GLSurfaceView, we would need to shutdown/init bgfx
           again as well and lose all context -->
        <activity
            android:name=".MainActivity"
            android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Support App Links -->
<!--            <intent-filter android:autoVerify="true">-->
<!--                <action android:name="android.intent.action.VIEW" />-->
<!--                <category android:name="android.intent.category.DEFAULT" />-->
<!--                <category android:name="android.intent.category.BROWSABLE" />-->
<!--                <data-->
<!--                    android:scheme="https"-->
<!--                    android:host="app.cu.bzh" />-->
<!--            </intent-filter>-->
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.voxowl.blip.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Push notifications -->
        <service
            android:name=".CubzhFirebaseInstanceIDService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Notification small icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_blip" />

        <!-- Passkey-->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <!-- Reminder local notifications -->
        <receiver android:name=".ReminderNotificationReceiver"/>

        <!-- Site association -->
        <meta-data android:name="asset_statements" android:resource="@string/asset_statements" />

    </application>

</manifest>
